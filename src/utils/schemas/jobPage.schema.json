{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "VacancyRecognizeResult",
  "type": "object",
  "required": [
    "statusCode",
    "status",
    "statusDetails"
  ],
  "properties": {
    "statusCode": {
      "type": "integer",
      "enum": [200, 400, 500],
      "description": "HTTP-style status code representing the answer. 200 if the document contains a job posting; 400 if the document does not contain a job posting or you cannot recognize the job from the document; 500 if your internal error or you cannot open the document."
    },
    "status": {
      "type": "string",
      "description": "Text explanation for statusCode, e.g., 'OK', 'Bad Request', 'Internal Server Error' etc."
    },
    "statusDetails": {
      "type": "string",
      "description": "Short explanation of why the status was set"
    },
    "jobTitle": {
      "type": "string",
      "description": "Job title from the document or 'not available'"
    },
    "jobDescription": {
      "type": "string",
      "description": "Short description of the job from the document or 'not available'"
    },
    "keywords": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Array of keyword strings for the job posting from the document"
    },
    "company": {
      "type": "string",
      "description": "Company name from the document or 'not available'"
    },
    "location": {
      "type": "string",
      "description": "Job location from the document or 'not available'"
    },
    "employmentType": {
      "type": "string",
      "description": "Employment type (e.g. Full Time, Part Time) from the document or 'not available'"
    },
    "remotePolicy": {
      "type": "string",
      "description": "Remote policy (e.g. Remote, Hybrid, Onsite) from the document or 'not available'"
    },
    "seniority": {
      "type": "string",
      "description": "Job seniority (e.g. Junior, Senior, Lead) from the document or 'not available'"
    },
    "responsibilities": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Optional/Main job responsibilities as array of strings"
    },
    "requirements": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Optional/Main job requirements as array of strings"
    },
    "nice_to_have": {
      "type": "string",
      "description": "Optional/Nice-to-have requirements 'not available'"
    },
    "languages": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Optional/Languages required or preferred for the job"
    },
    "salary": {
      "type": "string",
      "description": "Optional/Salary info or Compensation info or 'not available'"
    }
  },
  "additionalProperties": false
}